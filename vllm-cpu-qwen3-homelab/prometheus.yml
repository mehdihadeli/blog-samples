global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
- /etc/prometheus/alerts.yml

scrape_configs:
- job_name: vllm
  metrics_path: /metrics
  static_configs:
  - targets: [ "vllm:8000" ]
  scrape_interval: 10s
