apiVersion: 1

providers:
  - name: agentgateway
    orgId: 1
    folder: agentgateway
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
